machine:
  services:
    - docker

dependencies:
  override:
    - echo "No deps"

test:
  override:
    - echo "No tests yet..."

deployment:
  hub:
    branch: master
    owner: vispdevteam
    commands:
      - ./ci-build.sh
