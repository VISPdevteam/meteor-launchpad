machine:
  services:
    - docker

dependencies:
  override:
    - echo "No deps"

test:
  override:
    - echo "No tests yet..."

deployment:
  hub:
    branch: master
    commands:
      - ./ci-build.sh
